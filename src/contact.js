import {text} from './index'

const showContact = () => {
    text.innerText= `Phil Jackson
                        LA
                        (California)`
    content.appendChild(text)
}

export {showContact}
