import {text} from './index'

const showContact = () => {
    text.innerText= `Luis Navarro
                        
                        (Madrid)`
    content.appendChild(text)
}

export {showContact}
